extends layout


block content 
    main.container
        h1= title
        form(action=`/edit/${task.id}`, method="POST")
            label(for="title") Title
            input(type="text", name="title", id="title", value=task.title required)
            button.add-button(type="submit") Update
        a.back-button(href="/") Back